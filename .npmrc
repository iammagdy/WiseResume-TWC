# Suppress engine version mismatch warnings from @capacitor/cli (requires Node >=22,
# Replit runs Node 20 LTS). The web build is unaffected by this restriction.
engine-strict=false
legacy-peer-deps=true
# Skip Puppeteer browser download — not needed in the Replit web environment.
PUPPETEER_SKIP_DOWNLOAD=true
