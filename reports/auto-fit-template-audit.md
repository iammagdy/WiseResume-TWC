# Auto-fit Template Audit

Generated by `src/components/templates/__tests__/autoFitTemplateAudit.test.ts`.

Static analysis of every resume template against the set of utility classes scaled by `buildCompactScaleBlock` in `src/lib/templateCustomization.ts`. Anything listed below contributes to vertical height but is NOT scaled by `--compact-scale`, so it can prevent auto-fit from converging on the user-requested page count.

## Categories

- **Unscaled vertical utility**: prefix or spacing token not covered by `buildCompactScaleBlock` — high priority, easy fix (extend `SPACING_REM` / `spacingProps`).
- **Arbitrary font size** (`text-[10px]`): bypasses the `text-xs..5xl` scaling list. Tracked in the existing follow-up task "Add an arbitrary text size scale (e.g. text-[10px]) to compact mode".
- **Arbitrary spacing** (`mb-[12px]`, `py-[6px]`): bypasses the spacing-class scaling.
- **Fixed heights** (`h-12`, `min-h-[792px]`, `max-h-screen`): hard pixel/viewport heights that do not shrink with the rest of the layout. `min-h-[792px]` on the outer page sentinel is intentional and can be ignored.

## Per-template findings

### `src/components/templates/AcademicTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-[792px]` — fixed/relative height "min-h-[792px]" — does not respond to --compact-scale

### `src/components/templates/BankingTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/BentoTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/BoldTypeTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/BrutalistTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/ClassicTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/CleanTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/CompactTemplate.tsx`

**Arbitrary font sizes** (bypass text-* scaling):
- `text-[10px]` — arbitrary font-size token "10px" — not scaled by --compact-scale

**Fixed heights** (do not shrink):
- `min-h-[792px]` — fixed/relative height "min-h-[792px]" — does not respond to --compact-scale

### `src/components/templates/ConsultingTemplate.tsx`

**Fixed heights** (do not shrink):
- `h-1` — fixed/relative height "h-1" — does not respond to --compact-scale

### `src/components/templates/CreativeTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-full` — fixed/relative height "min-h-full" — does not respond to --compact-scale
- `h-16` — fixed/relative height "h-16" — does not respond to --compact-scale
- `h-0.5` — fixed/relative height "h-0.5" — does not respond to --compact-scale
- `h-2` — fixed/relative height "h-2" — does not respond to --compact-scale

### `src/components/templates/DataScienceTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/DesignerTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-full` — fixed/relative height "min-h-full" — does not respond to --compact-scale
- `h-20` — fixed/relative height "h-20" — does not respond to --compact-scale

### `src/components/templates/DeveloperTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/DevOpsTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/ElegantTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-[792px]` — fixed/relative height "min-h-[792px]" — does not respond to --compact-scale
- `h-1.5` — fixed/relative height "h-1.5" — does not respond to --compact-scale

### `src/components/templates/ExecutiveTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/FederalTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/HealthcareTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-[792px]` — fixed/relative height "min-h-[792px]" — does not respond to --compact-scale
- `h-4` — fixed/relative height "h-4" — does not respond to --compact-scale

### `src/components/templates/LegalTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/MarketingTemplate.tsx`

**Fixed heights** (do not shrink):
- `h-1.5` — fixed/relative height "h-1.5" — does not respond to --compact-scale

### `src/components/templates/MinimalTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/ModernTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/PortfolioTemplate.tsx`

**Fixed heights** (do not shrink):
- `h-0.5` — fixed/relative height "h-0.5" — does not respond to --compact-scale

### `src/components/templates/ProductTemplate.tsx`

**Fixed heights** (do not shrink):
- `h-1` — fixed/relative height "h-1" — does not respond to --compact-scale

### `src/components/templates/ProfessionalTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/SalesTemplate.tsx`

**Fixed heights** (do not shrink):
- `min-h-[792px]` — fixed/relative height "min-h-[792px]" — does not respond to --compact-scale

### `src/components/templates/SwissTemplate.tsx` — ✅ all vertical utilities covered

### `src/components/templates/WiseResumeClassicTemplate.tsx` — ✅ all vertical utilities covered

## Summary

- Templates audited: **28**
- Unscaled vertical utility occurrences: **0**
- Arbitrary font sizes: **1**
- Arbitrary spacing: **0**
- Fixed-height utilities: **17**
