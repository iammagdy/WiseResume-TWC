/**
 * LEGACY STUB — pending Appwrite migration.
 *
 * The Supabase-generated `Database` type is no longer needed; the
 * stub `supabase` client is permissively typed. This file exports a
 * minimal placeholder so any leftover `import type { Database }`
 * declarations continue to compile.
 */

/* eslint-disable @typescript-eslint/no-explicit-any */
export type Database = any;
export type Json = any;
