/**
 * Stub — there are no local BYOK keys to migrate any more.
 * The function is kept callable so existing call sites are still valid.
 */
export async function migrateLocalKeys(): Promise<void> {
  // intentionally empty
}
