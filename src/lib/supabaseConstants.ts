/**
 * LEGACY STUB — pending Appwrite migration.
 * Supabase URL / anon key constants are intentionally empty. Anything
 * that fetches against these values will fail; non-AI features are
 * scheduled to be rebuilt on Appwrite.
 */

export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
export const EDGE_FUNCTIONS_URL = '';
export const EDGE_FUNCTIONS_ANON_KEY = '';
