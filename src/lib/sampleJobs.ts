/**
 * @deprecated This file is no longer used in production.
 * Sample jobs are no longer written to the database.
 * The "See Sample Jobs" feature in the Job Tracker now shows a
 * read-only demo dialog instead. This export is kept only for
 * backward compatibility with existing test mocks.
 */
export const sampleJobs: never[] = [];
