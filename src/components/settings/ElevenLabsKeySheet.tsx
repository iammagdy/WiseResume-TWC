/**
 * Stub component — ElevenLabs voice key has been removed.
 * Kept so existing imports compile. Renders nothing.
 */

interface ElevenLabsKeySheetProps {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export function ElevenLabsKeySheet(_props: ElevenLabsKeySheetProps) {
  return null;
}

export default ElevenLabsKeySheet;
