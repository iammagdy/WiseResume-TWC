/**
 * Stub — AI Voice settings have been removed (no more ElevenLabs).
 * Kept so existing imports compile. Renders nothing.
 */

interface AIVoiceSectionProps {
  [key: string]: unknown;
}

export function AIVoiceSection(_props: AIVoiceSectionProps) {
  return null;
}

export default AIVoiceSection;
